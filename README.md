# python_doc
A very simple Python / Word templating example.

This depends on the [`docxtpl`](https://docxtpl.readthedocs.io/en/latest/) Python module.

Usage: `python3 generate.py -c <contextfile> -t <templatefile> -o <outputfile>`
